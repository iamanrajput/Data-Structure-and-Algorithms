import java.util.*;

public class hashmapOperation {
    public static void main(String[] args) {

        HashMap<String, Integer> hm = new HashMap<>();
        // to add element in the haspmap
        hm.put("India", 150);
        hm.put("China", 100);
        System.out.println(hm);

        // // to get element
        // int population = hm.get("India")
        // System.out.println(population);

        // // to check whether element is present or nor
        // System.out.println(hm.containsKey(key : "India")); // true
        // System.out.println(hm.containsKey(key : "Russia")); // false

        // // to remove element
        // System.out.println(hm.remove(key : "China"));
        // System.out.println(hm);
    }
}
